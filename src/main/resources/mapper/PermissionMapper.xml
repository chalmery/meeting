<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.yangcc.mapper.PermissionMapper">
    <!--根据用户名查询用户权限集合-->
    <select id="findPermissionValueByUserId" resultType="string" parameterType="int">
        select p.name
        from m_user u,m_role r,m_user_role ur,
             m_menu m,m_role_menu rm,m_permission p,m_permission_menu pm
        where u.id=ur.user_id and r.id=ur.role_id and r.id=rm.role_id
          and m.id=rm.menu_id and m.id=pm.menu_id and p.id=pm.permission_id
          and u.id=#{id};
    </select>
</mapper>